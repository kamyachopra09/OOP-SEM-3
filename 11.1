#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>

using namespace std;

int main() {
    vector<int> v = {6, 1, 2, 3, 4, 5, 6, 7, 8, 9};

    cout << "Initial Vector: " << endl;
    for (int val : v) {
        cout << val << " ";
    }
    cout << endl;

    for (int &val : v) {
        val = val * 10;
    }

    cout << "Multiplied by 10: " << endl;
    for (int val : v) {
        cout << val << " ";
    }
    cout << endl;

    return 0;
}
